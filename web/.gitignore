/node_modules
/.next/
/out/
/build

next-env.d.ts
*.tsbuildinfo

.env*.local
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel

# Generated by scripts/build-seo-files.ts on every build. Derived from content already in
# the repo, so committing them would only create merge conflicts. (lib/image-dimensions.json
# is committed instead because producing that one needs network access.)
/public/sitemap.xml
/public/fr/
/public/en/
/public/index.xml
/public/_redirects
